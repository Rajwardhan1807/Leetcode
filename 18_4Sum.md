# 18. 4Sum

## Problem Statement

Given an integer array `nums` and an integer `target`, return all the **unique quadruplets**

```text
[nums[a], nums[b], nums[c], nums[d]]
```

such that:

* `a`, `b`, `c`, and `d` are distinct indices.
* The sum of the four numbers equals `target`.

The solution must not contain duplicate quadruplets.

---

## Example 1

```text id="ghx4pu"
Input:
nums = [1,0,-1,0,-2,2]
target = 0

Output:
[[-2,-1,1,2],[-2,0,0,2],[-1,0,0,1]]
```

## Example 2

```text id="myu9g1"
Input:
nums = [2,2,2,2,2]
target = 8

Output:
[[2,2,2,2]]
```

---

## Approach

### Sorting + Two Pointers

This problem extends the idea used in **3Sum**.

1. Sort the array.
2. Fix the first element (`i`).
3. Fix the second element (`j`).
4. Use two pointers:

   * `left = j + 1`
   * `right = n - 1`
5. Calculate the sum of the four numbers.
6. If:

   * Sum equals target → store the quadruplet.
   * Sum is smaller → move `left`.
   * Sum is larger → move `right`.
7. Skip duplicate values for `i`, `j`, `left`, and `right`.

---

## Why This Works

Sorting lets us efficiently adjust the last two numbers using the two-pointer technique.

For example:

```text id="t1vdz3"
[-2,-1,0,0,1,2]
```

Fix:

```text id="wrvjlwm"
i = -2
j = -1
```

Use two pointers:

```text id="w5jlwm"
-2  -1   0   0   1   2
         ^           ^
      left        right
```

Current sum:

```text id="oizd3u"
-2 + (-1) + 1 + 2 = 0
```

Quadruplet found:

```text id="k2p41b"
[-2,-1,1,2]
```

Move both pointers while skipping duplicates.

---

## Code

```cpp id="o1vaz4"
class Solution {
public:
    vector<vector<int>> fourSum(vector<int>& nums, int target) {

        vector<vector<int>> ans;
        int n = nums.size();

        sort(nums.begin(), nums.end());

        for(int i = 0; i < n - 3; i++) {

            if(i > 0 && nums[i] == nums[i - 1])
                continue;

            for(int j = i + 1; j < n - 2; j++) {

                if(j > i + 1 && nums[j] == nums[j - 1])
                    continue;

                int left = j + 1;
                int right = n - 1;

                while(left < right) {

                    long long sum = (long long)nums[i] + nums[j] + nums[left] + nums[right];

                    if(sum == target) {

                        ans.push_back({nums[i], nums[j], nums[left], nums[right]});

                        left++;
                        right--;

                        while(left < right && nums[left] == nums[left - 1]) left++;
                        while(left < right && nums[right] == nums[right + 1]) right--;
                    }
                    else if(sum < target) {
                        left++;
                    }
                    else {
                        right--;
                    }
                }
            }
        }

        return ans;
    }
};
```

---

## Dry Run

### Input

```text id="ulgbg5"
nums = [1,0,-1,0,-2,2]
target = 0
```

After sorting:

```text id="vk4dwu"
[-2,-1,0,0,1,2]
```

Fix:

```text id="iw7bhs"
i = -2
j = -1
```

Two pointers:

```text id="jlwmq7"
left = 0
right = 2
```

Current sum:

```text id="qhb9mw"
-2 + (-1) + 1 + 2 = 0
```

First quadruplet:

```text id="ps77o8"
[-2,-1,1,2]
```

Continue searching:

```text id="dglgm3"
[-2,0,0,2]

[-1,0,0,1]
```

Final Answer:

```text id="m5xyxy"
[[-2,-1,1,2],[-2,0,0,2],[-1,0,0,1]]
```

---

## Complexity Analysis

### Time Complexity

```text id="gy2l1g"
O(N³)
```

* Sorting takes **O(N log N)**.
* Two nested loops plus a two-pointer traversal result in **O(N³)**.

### Space Complexity

```text id="7mhfc7"
O(1)
```

Ignoring the space used for storing the output.

---

## Key Concepts

* Sorting
* Two Pointers
* Nested Loops
* Duplicate Handling

---

## LeetCode

**Problem #18 – 4Sum**

### Stats

* **Difficulty:** Medium
* **Acceptance Rate:** 40.9%
* **Topics:** Array, Two Pointers, Sorting

### Learning Outcome

This problem extends the **3Sum** approach by fixing two numbers instead of one and then applying the **Two Pointer** technique to efficiently find the remaining two numbers while avoiding duplicate quadruplets.
